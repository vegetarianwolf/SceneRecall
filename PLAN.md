# SceneRecall v0.1：本地影视识别与检索应用

本文件同步用户在 2026-09-22 确认并要求实施的计划，替代原规划稿。项目已进入实现阶段；这里的验收指标是目标，使用方式见 [README.md](README.md)，实际通过项见 [验证记录](docs/VALIDATION.md)，不能据此宣称真实模型已通过验收。

## 1. 目标与交付范围

构建单用户本地网页应用，服务、识别资料和搜索索引保存在用户电脑上。用户自行配置 AI 接口，导入电影、动画或剧集，通过自然语言找回人物、动作、物体、位置或台词对应的片段，用于影评写作。

第一版贯通：**配置模型 → 导入视频与字幕 → 识别入库 → 自然语言检索 → 原片定位 → 纠错、收藏和引用**。

- 支持多作品，以及手动填写剧名、季、集和视频版本。
- 搜索结果包含作品、集数、时间范围、缩略图、台词或画面依据，点击播放原片对应位置。
- 支持修改识别描述、确认角色姓名、添加别名、收藏片段、记录笔记和导出引用清单。
- 暂不实现自动剪辑、完整影评生成、多人协作、云同步、自动跨镜头人物识别和桌面安装包。
- 仓库现有视频仅作为**项目构建完成后的真实验收素材**；开发阶段使用合成短视频、人工记录和模型测试替身。

## 2. 入库流程与本地数据

### 两种字幕模式

| 模式 | 用户输入 | 应用行为 |
| --- | --- | --- |
| 画面内嵌字幕 | 视频、独立的字幕视觉模型配置 | 用小型视觉模型 API 识别烧录在画面中的字幕 |
| 外挂字幕 | 视频及字幕文件 | 必须提供字幕文件，缺失或解析失败时不能提交导入任务 |

- 外挂字幕支持 SRT、VTT、ASS；保留原文件，将显示文本与时间范围统一转换为内部记录。
- 内嵌字幕识别与画面理解独立运行、独立配置模型、独立重试。
- 内嵌字幕默认每秒采样两帧，默认裁剪画面下方 35%，用户可调整字幕区域；候选图像去重后，以带时间戳的多图批次交给字幕模型。
- 模型只转录实际可见文字；无法辨认的内容标记待复核。连续重复字幕合并，间隔后再次出现的同一句话保留为不同记录。
- 日中双语分别保存，不自动翻译或用转写内容替换画面原文。
- 容器独立字幕轨的自动提取、无字幕音轨的 ASR 转写列入后续版本。

### 画面识别

流程为：文件探测 → 镜头检测 → 分析窗口划分 → 抽帧 → 多模态模型分析 → 校验落盘 → 更新索引。

- 初始默认每个窗口最长 6 秒、均匀抽取 4 帧；不跨镜头拼成同一观察窗口。参数可调整，并随分析记录保存。
- 记录可见人物外观、物体、动作参与者、空间关系、时间范围和证据帧。
- 位置先使用画面左／中／右、前景／背景及对象间关系，精确检测框延后。
- 人物姓名默认未知，用户确认后建立角色映射；不同镜头中相同的局部编号不代表同一人物。
- 可见事实与推测分开保存；有限抽帧未覆盖的动作不能当成已确认事实。

### 数据持久化

- JSON 保存作品、视频资产、分析结果、运行信息和用户注释；JSONL 保存字幕顺序记录。当前实现将可重建的镜头分割缓存保存为 `shots.json` 数组，作为原计划 JSONL 镜头记录的等价文件化实现。
- 核心对象为作品／集、视频资产、镜头、分析窗口、视觉观察、字幕句、分析运行和用户注释。
- 视频默认引用原文件，不自动复制；移动后可以重新定位。资料备份与包含原视频的完整备份明确区分。
- 时间统一为相对视频播放起点的整数毫秒，使用实际媒体时间戳处理可变帧率。
- 每次分析产生独立版本；局部重跑成功后切换有效结果，保留旧版本，人工纠错不被覆盖。
- 文件先校验并原子提交，再更新索引。SQLite 和向量索引可由本地资料重建。
- 所有正式资料带格式版本；模型、提示词版本、输入参数和证据引用可追溯。

## 3. 模型接口、检索与技术结构

### 模型能力分离

用户创建连接配置，填写协议、Base URL、模型名及 API Key，再分配给各项能力；允许多个能力复用同一配置。

| 接口 | 输入与输出 | 初期实现 |
| --- | --- | --- |
| `VisionAnalyzer` | 带时间戳的多帧 → 视觉观察 | 多模态 LLM |
| `SubtitleRecognizer` | 字幕区域图片 → 可见文字及帧引用 | 独立配置的小型视觉模型 API |
| `TextEmbedder` | 文本 → 向量 | 用户配置的 embedding 接口 |
| `QueryInterpreter` | 自然语言 → 检索文本与条件 | 文本 LLM |
| `DecisionProvider` | 状态及限定问题 → 选择、评分或判断 | 传统 LLM，未来替换为 Jev |
| `AnswerComposer` | 候选证据 → 带引用的解释 | 模板为默认，LLM 为可选 |

Jev 用于查询分类、候选匹配评分和是否扩大搜索等决策；自由文本生成和视觉理解各自保留独立接口。[Jev 官方文档](https://docs.typesafe.ai/introduction)

- `DecisionProvider` 的契约不要求生成自由文本理由，避免未来替换仍依赖 LLM。
- 第一版支持 OpenAI-compatible 的图像消息、文本与 embedding 协议，按能力进行连接测试；其他供应商原生协议后续添加。
- API Key 由后端使用，支持系统凭据库、会话或环境变量方式；不进入资料文件、日志或导出包。需要持久保存时优先选择系统凭据库。
- 每个阶段显示所用模型和发送的数据类型；本地服务允许无 Key。

### 检索行为

采用：**条件过滤 + 字面召回 + 语义召回 → 排名融合 → 有限候选重排 → 证据结果**。

- 对字幕、视觉描述、动作关系和用户确认的角色别名分别建立检索记录。
- 中文采用一致的分词和规范化处理，准确台词另做子串匹配；跨字幕句的查询使用相邻上下文补充。
- 默认字面与语义各召回 50 条，使用 RRF 融合；对前 20 条进行可选决策重排，返回前 10 条。
- 对人物、动作和空间组合条件核对实际关系，不能仅凭关键词共现判定完整匹配。
- 区分完整匹配、部分匹配和未找到；解释中的引用必须指向已有记录。
- 更换检索 LLM 不重新识别；更换 embedding 只重建对应向量索引；检索不触发视觉或字幕识别请求。
- 未配置 embedding 时保留字面检索，并明确显示功能降级。

### 技术组成与本机 API

采用 **Python + FastAPI + Pydantic、React + TypeScript + Vite、FFmpeg/ffprobe、PySceneDetect、SQLite FTS5、LanceDB OSS**。后台任务使用单机 worker 和 SQLite 持久队列。

本机 API 覆盖模型配置及测试、媒体登记、入库任务、任务控制、检索、媒体播放、纠错与收藏、导出及索引重建；前端不直接访问模型供应商。工程接口见 [IMPLEMENTATION_CONTRACT.md](docs/IMPLEMENTATION_CONTRACT.md)。

- 服务仅监听本机，使用会话鉴权、来源检查和受限同源访问。
- 视频通过已登记的资产 ID 播放，支持范围请求；不开放任意文件路径读取。
- 不兼容浏览器的媒体按需生成播放代理，保持原片时间映射。
- 任务支持暂停、恢复、取消和失败重试；成功窗口复用，失败窗口单独处理。
- 提交前展示采样量和预计请求量，支持试运行、请求数及费用预算限制；没有价格信息时仅展示用量估算。

## 4. 实施顺序

| 阶段 | 交付与完成条件 |
| --- | --- |
| **M0：基础契约** | 建立项目、数据结构、模型能力接口、文件存储和测试替身；完成资料读写及基本索引重建 |
| **M1：媒体与外挂字幕** | 完成资料库、视频登记、播放器、镜头切分和强制字幕文件校验；可按台词定位合成视频 |
| **M2：AI 入库** | 完成内嵌字幕识别、画面理解、持久任务、用量统计、暂停恢复和局部重跑；两个识别任务独立保存结果 |
| **M3：自然语言检索** | 完成向量索引、中文检索、融合排序、决策接口和证据结果；验证模型切换不导致重复识别 |
| **M4：完整应用** | 完成纠错、角色命名、收藏笔记、引用导出、备份恢复、文件重定位和运行文档 |
| **最终验收** | 项目构建完成后，使用仓库现有视频及用户配置的真实 API，先验证短片段，再按预算测试整片 |

开发阶段不调用现有测试影片做抽帧或模型实验。缺少真实 API 凭据时继续完成替身测试；真实识别质量保持“待验收”，不能用模拟结果代替通过。

## 5. 测试、验收与默认约定

- **字幕模式**：外挂字幕缺失、格式损坏时阻止提交；内嵌模式验证空字幕帧、双语、连续重复、间隔重复、跨镜头字幕和识别失败。
- **识别证据**：结构、时间和引用有效；未知人物、未知文字及推测不被保存为已确认事实。
- **数据恢复**：中断、重启、局部重跑、原视频移动、索引删除重建后，资料与人工修改仍可恢复。
- **模型独立性**：切换检索模型不产生识别请求；切换字幕模型不重跑画面理解；切换 embedding 不重跑任何识别。
- **接口故障**：覆盖认证失败、限流、超时、不支持多图和不合法输出；有限重试，日志无 Key。
- **真实质量**：至少 30 条有目标检索问题和 10 条无答案问题；Top-5 命中率目标不低于 80%，不存在的引用必须为零。
- **字幕与定位**：对人工核对的清晰字幕评估逐字准确率、漏检率及时间误差；初始目标为逐字准确率不低于 95%、时间边界误差不超过 1 秒。
- **性能与成本**：在注明硬件的条件下，1 万条记录的本地候选召回目标不超过 1 秒；远端模型延迟单列，并记录整片请求量、费用和磁盘占用。

默认单用户、中文界面、当前 macOS 优先验证；具体模型由用户配置，不绑定供应商。上述质量指标是交付验收目标，需要真实测试证明。ASR、自动人物关联、Jev 实际接入和其他平台发布在后续版本推进。
